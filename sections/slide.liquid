<section class="slide">
  <div>
    <p class="frase-1">Embelezando não somente você</p>
    <p class="frase-2">Mas também sua casa!</p>
  </div>

  <div class="painel">

    <div class="primeiro-painel">
      <h1 class="primeiro-painel-titulo">Macramê para suporte de plantas</h1>
      <p class="primeiro-painel-desc">os queridinhos que dão um charme especial a sua garagem </p>
      <img
      src="{{ 'imagem-slide-1.png' | asset_url }}"
      alt="Macramê"
      class="imagem-primeiro-painel">

    </div>

    <div class="segundo-painel">
      <img
      src="{{ 'imagem-slide-2.png' | asset_url }}"
      alt="Macramê"
      class="imagem-primeiro-painel">
      <h1 class="segundo-painel-titulo">Painel de Macramê decorativo</h1>
      <p class="segundo-painel-desc">sua casa nunca mais será a mesma </p>

      <button>Venha Conferir!</button>
    </div>

  </div>

</section>


<style>
  .slide {
    padding: 0 80px;
    width: 100%;
    margin-top: 120px;
  }

  .slide .frase-1 {
    font-family: var(--bold);
    font-size: 70px;

    background: linear-gradient(90deg, var(--cor-verde-principal), var(--cor-verde-principal-hover));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .slide .frase-1::selection , .frase-2::selection {
    background: rgba(0, 0, 0, 0.15);
    color: inherit;
  }

  .slide .frase-2 {
    font-family: var(--bold);
    font-size: 70px;
    margin: 0 500px 30px;

    background: linear-gradient(90deg, var(--cor-verde-principal-hover), var(--cor-verde-principal));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .painel {
    display: flex;
    gap:20px;
    margin-top: 50px;

    margin-bottom: 150px;
  }

  .primeiro-painel {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;

    margin-top: 50px;
  }

  .segundo-painel {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .primeiro-painel .primeiro-painel-titulo {
    font-size: 35px;
    font-family: var(--bold);

    background: linear-gradient(90deg, var(--cor-verde-principal-hover), var(--cor-verde-principal));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .primeiro-painel .primeiro-painel-desc {
    font-size: 20px;
    font-family: var(--light);
  }

  .segundo-painel .segundo-painel-titulo {
    font-size: 35px;
    font-family: var(--bold);

    background: linear-gradient(90deg, var(--cor-verde-principal-hover), var(--cor-verde-principal));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .segundo-painel .segundo-painel-desc {
    font-size: 20px;
    font-family: var(--light);
  }

  .primeiro-painel-titulo::selection,
  .segundo-painel-titulo::selection,
  .primeiro-painel-desc::selection,
  .segundo-painel-desc::selection {
    background:rgba(0, 0, 0, 0.15);
    color: inherit;
  }

  .segundo-painel button {
    margin-top: 20px;

    width: 350px;
    height: 50px;
    border-radius: 50px;

    background-color: var(--cor-verde-principal);
    font-family: var(--bold);
    font-size: 20px;
  }

</style>
